# this file goes into Git so that if someone clones the repo,
# they know how to config. env variables

FLASK_APP=run.py

SECRET_KEY=your_secret_key

DATABASE_URL=postgresql+psycopg://username:password@localhost:5432/database_name